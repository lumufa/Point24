# scenes/ — Cocos 场景 (B3)

由 Cocos Creator 编辑器生成的 `.scene` 文件。计划:

- `Main.scene` — 主场景,承载 `GameStage` 组件
- `Loading.scene` — 启动 Loading(可选,包体小可直接进 Main)

建议在 Cocos 菜单栏「资源管理器」右键本目录 → 新建 → 场景来创建,不要手写 JSON。
